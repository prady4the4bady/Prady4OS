/* third_party/lwip-port/arch/sys_arch.h — PRADYOS lwIP sys layer (NO_SYS=1).
 * SYS_LIGHTWEIGHT_PROT=0 and no OS layer, so no sem/mutex/mbox/prot types are
 * needed; lwIP only requires sys_now() (provided by lwip_port.c). */
#ifndef PRADYOS_LWIP_ARCH_SYS_ARCH_H
#define PRADYOS_LWIP_ARCH_SYS_ARCH_H
#endif
