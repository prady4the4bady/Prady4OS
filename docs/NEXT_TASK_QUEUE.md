# NEXT_TASK_QUEUE — FIFO of ready-to-start work

**Operator directive 2026-08-23 §6.6.** The rule "task-switch during CI-wait,
never idle-stop" was correct but abstract, and abstract meant items got
re-picked or forgotten between sessions. This is the literal queue.

## How to use it

1. The moment a long QEMU campaign starts (which blocks all other QEMU use on
   this machine — §NON-NEGOTIABLE 12), **pop the top unclaimed item** and start
   its DDR or code-reading phase.
2. Mark it `[~]` with your session date when you start, `[x]` when its gate is
   green and pushed. Leave it `[ ]` if you only read.
3. **Append new work to the bottom**, never insert at the top — the ordering
   below encodes dependencies, and re-prioritising by insertion is how a queue
   stops being a queue.
4. Items marked **BLOCKED** name what unblocks them. Do not pop a blocked item.

Counts are generated by `tools/ci/status_report.sh`; do not hand-tally.

---

## Queue

### Immediate — release critical path
- [~] PR #6 → 3 CI greens on one tip → squash-merge into `dev/phase1`
- [ ] `dev/phase1` → 3 greens → fast-forward `main`
- [ ] Re-run the DDR-978 manual ISO walkthrough on **main's own** ISO
      (directive §5: verify the actual release candidate, not an earlier tip)
- [ ] Tag `v1.0.0`

### Group E — UI/UX (directive §3: "THIS IS NOT OPTIONAL")
Batch their DDRs in one pass first (§4.3), then implement.
- [x] PS/2 modifier keys: F-keys, arrows, Alt, Ctrl, Meta/Super — DDR-991,
      `smoke-modkeys` 5 arms, mutation-checked. Adds NSI 96 SYS_KEY_POLL.
      **Unblocks the next three items** (they need KMOD_* state a byte stream
      cannot carry).
- [ ] Super+M physical sovereign-mode toggle (`SYS_SET_MODE`)
- [ ] Alt-Tab with real modifier plumbing (upgrade from plain Tab, DDR-720)
- [ ] Ctrl+Alt+T launches a PRISM terminal window
- [ ] Per-window restore from dock (DDR-717 restores all)
- [ ] Window maximize at real display size (DDR-719 caps at 512×512).
      **Read DDR-975 §7 first** — the client-side resize-ack is the bug, not the WM
- [ ] Pointer resize handles on all edges (DDR-718 is bottom-right only)
- [ ] `SURF_EV_CLOSE` notification so an owner can save state
- [ ] OKLab horizon bands / animated mesh (DDR-716 deferred)
- [ ] vDSO callable ring-3 clock reader (`vdso_entry.asm`, IMP-C)
- [ ] Compositor double-map `PTE_SW_SHARED` audit
- [ ] OPEN-1 `smoke-surfdestroy` intermittent.
      **Start from this session's finding, do not re-derive**: its only captured
      failure hung at `SYSFSTAT OK` → `SYSREAD OK`, i.e. inside `sys_read`/
      `vfs_read`, *before any surface code ran*. It is misfiled as a compositor
      bug. That stall point is the same function DDR-981 caught frozen, and
      `[apfreeze]` is now a `GLOBAL_FORBIDDEN` sentinel, so a recurrence names
      itself. Re-check whether it still reproduces at all before instrumenting.

### Group F — AETHER roster (11 agents unbuilt)
- [ ] Wire `CAP_OCR` (1<<19), `CAP_EXEC` (1<<20), `CAP_SCENE` (1<<22),
      `CAP_NET_BROWSE` (1<<23) — **do these first**, they unblock four agents
- [ ] PRAX (shell_agent) spawnable — BLOCKED on `CAP_EXEC`
- [ ] LUMYN (research_agent) spawnable — BLOCKED on `CAP_NET_BROWSE`
- [ ] AHNIS (ocr_agent) spawnable — BLOCKED on `CAP_OCR`
- [ ] IRIS (vision_agent) spawnable — BLOCKED on `CAP_SCENE`
- [ ] F#66 `architect_agent` / F#67 `healer_agent` (RUFLO) / F#69 `inventor_agent`
- [ ] F#70 `tournament_agent` / F#71 subconscious world model / F#72 `verifier_agent`
- [ ] F#73 sovereign NL UI / F#74 capability discovery / F#75 lineage memory
- [ ] F#76 tamper-evident ledger / F#68 metric lockbox e2e
- [ ] Section 3C actions: READ_FILE, DELETE_FILE, SEND_IPC, QUERY_MEMORY,
      REWRITE_AGENT_CODE, PROPOSE_HYPOTHESIS, RUN_EXPERIMENT, EVOLVE_GENOME
- [ ] S3 + S7 invariant arms — BLOCKED on F#66–F#72

### Group D — userspace
- [ ] init-driven fork+execve **respawn** of PRISM (the only genuinely unbuilt
      part of ADR-024 §D5 — `run` itself was never disabled, DDR-973 §7)
- [ ] `argv`/`envp` marshalling in `sys_execve`
- [ ] PRISM readline / line discipline / echo
- [ ] pipes, redirection, quoting, job control (`&`, `wait`, `fg`/`bg`), scripting
- [ ] `SYS_MPROTECT`, `SYS_POLL`, `SYS_FUTEX`
- [ ] pthreads (`clone(CLONE_VM|CLONE_FILES|CLONE_THREAD)`)
- [ ] 6-arg `sys_mmap` ABI, file-backed `mmap` + `msync`
- [ ] dynamic linking (`ld.so`), full `io_uring`, full POSIX `sigaction`

### Group A / B / C — kernel, storage, net
- [ ] I/O APIC migration (Group A + B#9 share this)
- [ ] SMEP/SMAP, kernel W^X identity-alias removal, `#MC` handler, KASLR
- [ ] `sched_block_on_timeout`, lock-contention instrumentation
- [ ] SFS: on-disk free-tree persistence, CoW GC, extent overflow, >512 slots, quotas
- [ ] ext4 write, NUMA affinity, NAS 3-lane scheduler, PMM policy
- [ ] NVMe completion IRQ (B#1) — was BLOCKED on B#3, **now unblocked (DDR-981)**
- [ ] Networking: UDP, epoll/select on sockets, `SYS_NET_REVOKE`, TAP, IPv6, TLS shim

### Group G / H
- [ ] Phase 9.1–9.6 assembly optimisation — profile first, each needs a measured baseline
- [ ] `prad` package manager (NSI 88–90; 87 is `SYS_READ_AUDIT`, §INV.12)
- [ ] Full invariant suite S1–S8

### Infrastructure (directive §6) — status
- [x] §6.1 persistent campaign runner — `tools/ci/campaign.sh`
- [x] §6.2 failure auto-classifier — `tools/ci/classify_failure.sh`
- [x] §6.3 fast-lane tier + `make smoke-fast` — tier is a 4th column in `gate_shards.txt`
- [x] §6.4 shard matrix 6 → 10 (makespan 38.6 → 20.8 min)
- [x] §6.5 generated status dashboard — `tools/ci/status_report.sh`
- [x] §6.6 this queue
- [ ] §6.7 skip regenerating unchanged disk images — **NOT DONE, named rather
      than silently deferred** (directive §2). The recipes are `.PHONY` and
      always re-run; making them content-hash-conditional is the one item in §6
      whose failure mode is a *stale image silently used by a gate*. The
      directive argues the gate's own assertions would catch that, which is true
      for a gate that asserts on fixture content, but not for one that asserts
      only on a boot sentinel. Worth doing with a hash of the declared inputs
      and a forced rebuild whenever `$(KERNEL_BIN)` changes; ~30 min, low
      payoff per run compared to §6.4's makespan halving. Do it after the
      release path is clear.
